# 🚀 WinAntidote  
### *Абсолютный титан восстановления Windows после цифрового апокалипсиса*  
![Эпичный баннер WinAntidote](https://raw.githubusercontent.com/GregoryBale/WinAntidote/refs/heads/main/forgithub/images/Screenshot_1.png)  
*Ваш Windows пал под натиском вирусов? Рабочий стол пуст, сеть мертва, управление потеряно? **WinAntidote** — это не просто скрипт, а Python-машина возмездия, которая врывается в хаос, уничтожает последствия вредоносного ПО и воскрешает систему с хирургической точностью. Один запуск — и вы снова король своей машины. Добро пожаловать в эпоху цифрового триумфа!*

---
## 🌍 Веб сайт нашего продукта:
[Перейти на сайт](https://antidotegb.netlify.app/)

## 🌌 Легенда рождения  
Всё началось в тёмные времена: мой компьютер стал ареной войны с вирусами. Диспетчер задач исчез, интернет испарился, а система насмехалась над каждым кликом. Из этого хаоса я выковал **WinAntidote** — ответ цифровым захватчикам, созданный в боях с поломанными Windows. Теперь он доступен в двух версиях: консольной и графической — выбирай своё оружие для битвы с цифровым злом!

---

## 🌟 Что творит этот монстр?  
**WinAntidote** — это ультимативный восстановитель Windows, переосмысленный и усиленный до предела. Он не охотится за вирусами, а ликвидирует их следы, возвращая систему в строй. Вот его полный арсенал:

### 🛠️ Освобождение системных инструментов  
- **Диспетчер задач (`enable_task_manager`)**: Разрушает `DisableTaskMgr` в реестре, создаёт ярлык и проверяет через `psutil`.  
- **Редактор реестра (`enable_registry_editor`)**: Уничтожает `DisableRegistryTools`, добавляет ярлык с логированием.  
- **Командная строка и PowerShell (`enable_cmd_and_powershell`)**: Снимает `DisableCMD`, активирует PowerShell через `Set-ExecutionPolicy`.  
- **Панель управления (`enable_control_panel`)**: Убирает `NoControlPanel` и восстанавливает через `shell:::`.  
- **USB-накопители (`enable_usb_storage`)**: Возвращает `USBSTOR` с настройкой запуска вручную.  

### 🎨 Восстановление интерфейса  
- **Иконки рабочего стола (`show_desktop_icons`)**: Сносит `NoDesktop`, перезапускает Explorer через `psutil`.  
- **Кнопка "Пуск" (`enable_start_button`)**: Чинит меню, убирая `NoStartMenuPinnedList`.  
- **Контекстное меню (`enable_right_click`)**: Восстанавливает через `NoViewContextMenu` с логированием.  
- **Системный трей (`enable_system_tray`)**: Убирает `NoTrayItemsDisplay`, проверяет через `win32gui`.  
- **Панель задач (`restore_taskbar`)**: Использует `win32gui` для прямого восстановления API.  
- **Снимок экрана (`restore_desktop_via_screenshot`)**: Анализирует GUI через `pyscreeze`.  

### 🌐 Сетевое воскрешение  
- **Сброс сетевых настроек (`reset_network_configuration`)**: Выполняет `netsh`, `ipconfig`, `advfirewall reset` с асинхронностью (`asyncio`).  
- **Очистка DNS (`clean_dns_cache`)**: Сбрасывает через `ipconfig /flushdns` с прогрессом (`tqdm`).  
- **Сброс прокси (`reset_proxy_settings`)**: Уничтожает фальшивки через `netsh winhttp`.  
- **Включение адаптеров (`enable_network_adapters`)**: Активирует через PowerShell с логированием.  
- **Асинхронная сеть (`restore_network_async`)**: Проверяет через `pypsexec` и `asyncio`.  

### 🛡️ Защита и укрепление  
- **Windows Defender (`restore_antivirus`)**: Запускает `WinDefend`, включает защиту через `Set-MpPreference`.  
- **Брандмауэр (`restore_windows_firewall`)**: Настраивает `MpsSvc`, сбрасывает профили через `netsh`.  
- **Доступ к обновлениям (`restore_windows_update_access`)**: Убирает `DisableWindowsUpdateAccess`, синхронизирует время через `win32api`.  
- **Службы (`restore_critical_services`)**: Восстанавливает `Dnscache`, `EventLog`, `RpcSs` через `wmi`.  
- **Самозащита (`enable_self_protection`)**: Блокирует прерывания через `ctypes` с обработкой ошибок.  

### 🧹 Глубокая очистка и ремонт  
- **Временные файлы (`clean_temp_files`)**: Уничтожает мусор в `Temp` через `pathlib` и `shutil`.  
- **Ассоциации файлов (`restore_file_associations`)**: Чинит `.exe`, `.bat`, `.cmd` через `winreg`.  
- **Автозагрузка (`remove_from_startup`)**: Вырезает угрозы через `psutil`.  
- **Клавиатура (`clear_keyboard_hooks`)**: Снимает перехваты через `pynput`.  
- **Системный кэш (`clean_system_cache`)**: Очищает `SoftwareDistribution` с прогрессом (`tqdm`).  
- **Шрифты (`restore_system_fonts`)**: Восстанавливает через `pathlib`.  
- **Переменные (`restore_environment_variables`)**: Чинит PATH через `winreg`.  

### 💾 Ремонт загрузки и восстановления  
- **Загрузочный сектор (`repair_boot_sector`)**: Чинит через `bootrec` с бэкапом BCD.  
- **Безопасный режим (`enable_safe_mode`)**: Убирает `AlternateShell` через `winreg`.  
- **Опции восстановления (`enable_recovery_options`)**: Настраивает `bcdedit` с логированием.  
- **Точка восстановления (`create_restore_point`)**: Создаёт через `pywin32`.  
- **Теневые копии (`restore_from_shadow_copies`)**: Извлекает через `pyvshadow`.  

### 🔍 Уникальные инновации  
- **Обход блокировок (`bypass_input_blocks`)**: Эмулирует ввод через `pynput`.  
- **Звук (`restore_audio`)**: Перезапускает через `sounddevice`.  
- **Мониторинг (`monitor_system_changes`)**: Отслеживает через `watchdog`.  
- **Буфер обмена (`restore_clipboard`)**: Чинит через `pyperclip`.  
- **Профиль (`create_temp_safe_profile`)**: Создаёт безопасное окружение через `subprocess`.  
- **Логи (`analyze_system_logs`)**: Парсит `.evtx` через `pyevtx`.  
- **GUI-утилиты (`restore_system_utilities_gui`)**: Автоматизирует через `pywinauto`.  
- **Трей (`system_tray_monitor`)**: Мониторит через `pystray`.  

### 📜 Документация и отчёты  
- **Отчёт (`create_recovery_report`)**: Детальный лог в TXT с расширенными рекомендациями.  
- **Резервные копии (`create_antidot_backup`)**: Дублирует скрипт и реестр.  
- **QR-отчёт (`generate_qr_report`)**: Создаёт QR-код через `pylibdmtx`.  

---

## 🎨 Графическая версия — мощь и скорость  
Помимо консольной мощи, **WinAntidote** теперь доступен в **графической версии**!  
- **Скорость**: Более быстрая и оптимизированная работа.  
- **Интерфейс**: Написана на **Tkinter** — лёгкой и эффективной библиотеке (зачем усложнять, когда можно побеждать?).  
- **Демо**: ![Скриншот GUI](https://raw.githubusercontent.com/GregoryBale/WinAntidote/refs/heads/main/forgithub/images/Screenshot_4.png).  

**Важно**: Обязательно дождись завершения работы программы — появится надпись «Восстановление завершено» (![скриншот](https://raw.githubusercontent.com/GregoryBale/WinAntidote/refs/heads/main/forgithub/images/Screenshot_3.png)). Не прерывай процесс, дай машине возмездия закончить бой!

---

## 🔥 Почему WinAntidote — твой цифровой бог?  
| Особенность                | Почему это эпично?                              |  
|----------------------------|------------------------------------------------|  
| **Молниеносный удар**      | Один запуск — и система в строю.               |  
| **Графика или консоль**    | Выбирай свой стиль боя.                        |  
| **Лёгкий как перо**        | Без установок, чистая Python-мощь.             |  
| **Открытый код**           | Твой для улучшений и экспериментов.            |  
| **Непробиваемый щит**      | Самозащита от сбоев и прерываний.              |  

---

## 🛠️ Как овладеть этой мощью?  
Готов воскресить свой ПК? Вот твой план:  

1. **Скачай**: Хватай релиз из [Releases](https://github.com/GregoryBale/WinAntidote/releases).  
2. **Выбери версию**:  
   - **Консоль**: `WinAntidote.py` → Правый клик → «Запуск от имени администратора» (или через `pyuac`).  
   - **GUI**: `WinAntidoteGUI.exe` → Просто кликни и побеждай.  
3. **Дождись**: Следи за процессом (в консоли — цветной вывод через `colorama`, в GUI — прогресс-бар).  
4. **Перезагрузи**: Завершай победу рестартом.  

### 💡 Тактические хитрости  
- **Комбо-атака**: После WinAntidote ударь антивирусом для полного очищения.  
- **Мобильный арсенал**: Носи на флешке для спасения любого ПК.  
- **Читай отчёт**: TXT на рабочем столе раскроет все детали восстановления.  

---

## 🎯 Функции, которые взрывают мозг  
![Стабильность](https://img.shields.io/badge/Статус-Стабильно-green?style=for-the-badge) ![Python 3.9+](https://img.shields.io/badge/Python-3.9+-blue?style=for-the-badge) ![Лицензия MIT](https://img.shields.io/badge/Лицензия-MIT-red?style=for-the-badge)  
![Windows 10/11](https://img.shields.io/badge/Windows-10%2F11-orange?style=for-the-badge) ![Скачивания](https://img.shields.io/github/downloads/GregoryBale/WinAntidote/total?style=for-the-badge)

### Ключевые суперсилы  
- **Самозащита (`enable_self_protection`)**: Непробиваем через `ctypes`.  
- **Сетевое возрождение**: Полный сброс с асинхронностью (`asyncio`).  
- **Глобальный контроль**: От загрузки до GUI — всё под твоим командованием.  
- **Логи нового уровня**: TXT с деталями и рекомендациями.  
- **Админ-режим (`force_admin_privileges`)**: Автоматика через `pyuac`.  

### Уникальные фишки  
- **Монитор в трее (`system_tray_monitor`)**: Следит за системой через `pystray`.  
- **Анализ логов (`analyze_system_logs`)**: Копает глубже через `pyevtx`.  
- **Теневое спасение (`restore_from_shadow_copies`)**: Возвращает прошлое через `pyvshadow`.  
- **GUI-магия (`restore_system_utilities_gui`)**: Автоматизация через `pywinauto`.  
- **Прогресс (`simulate_long_recovery`)**: Визуализация через `tqdm`.  

---

## 📸 Докажи свою мощь  
![Демо](https://raw.githubusercontent.com/GregoryBale/WinAntidote/refs/heads/main/forgithub/images/Screenshot_2.png)  
*Смотри, как WinAntidote возвращает жизнь в мёртвую систему — это эпично!*  

### До и после  
| Состояние           | До WinAntidote            | После WinAntidote         |  
|---------------------|---------------------------|---------------------------|  
| Диспетчер задач     | Пленён                    | Свободен и готов          |  
| Рабочий стол        | Мёртвая пустыня           | Живой и яркий            |  
| Интернет            | Отключён                  | Летает как ракета         |  
| Звук                | Тишина                    | Чистый и громкий         |  
| Безопасность        | Уязвима                   | Непробиваема             |  

---

## 🧠 Технологии под капотом  
WinAntidote — это Python-монстр, собранный из мощных библиотек:  
- **`Tkinter`**: Графическая оболочка — быстрая и лёгкая.  
- **`ctypes`**: Низкоуровневый доступ к Windows.  
- **`psutil`**: Управление процессами и ресурсами.  
- **`winreg`**: Мастер реестра.  
- **`subprocess`**: Команды с железной хваткой.  
- **`pynput`**: Снятие перехватов ввода.  
- **`shutil`/`pathlib`**: Очистка и работа с файлами.  
- **`wmi`**: Глубокий анализ системы.  
- **`pywin32`**: API Windows на максимум.  
- **`asyncio`**: Асинхронная мощь.  
- **`colorama`**: Цветная эпичность в консоли.  

Полный список технологий — в коде. Копай глубже!

---

## 🤝 Вступай в революцию  
WinAntidote — это наш общий проект. Присоединяйся:  

- **Форкни**: Делай своим.  
- **Пулл-реквесты**: Добавляй свои идеи.  
- **Баги**: Нашёл косяк? Пиши [в тг](https://t.me/gregorybale).  
- **Распространяй**: Спасай ПК друзей.  

### Планы на будущее  
- [x] GUI — уже здесь!  
- [ ] Интеграция с антивирусами для комбо-удара.  
- [ ] Мультиязычность для мировой славы.  
- [ ] Автоматический откат изменений при сбоях.  
- [ ] Твои идеи — давай зажжём!  

---

## 💬 Связь с создателем  
Создано с огнём и 💪 Грегори Бейлом.  
- **Telegram**: [@gregorybale](https://t.me/gregorybale) — Пиши 24/7!  
- **Сайт**: [gregorybale.site](https://gregorybale.site) — Мои проекты и душа.  
- **Звезда репо**: Спас твой ПК? Кинь ⭐ — это топливо для меня!  

---

## ⚠️ Важное предупреждение  
WinAntidote — мастер восстановления, а не истребитель вирусов. Он чинит систему, но не удаляет угрозы. После запуска используй антивирус (Windows Defender, Malwarebytes) для финального удара. Это мощное оружие — используй с умом!

---

## 🎉 Отзывы (пока мечты!)  
> «Мой ПК был трупом — ни звука, ни сети. WinAntidote оживил его за минуты. Легенда!»  
> — СпасённыйЮзер  

> «Прощай, переустановка! WinAntidote вернул мой Windows. Грегори — бог кода!»  
> — ФанатЧистоты  

---

## 🌍 Зачем это миру?  
В эпоху, где вирусы поджидают за каждым кликом, WinAntidote — твой меч, щит и верный союзник. Это не просто ремонт — это цифровое возрождение. Скачивай, запускай и покажи хаосу, кто здесь главный.

---

### *«Когда Windows рушится, WinAntidote восстаёт. Твой новый шанс начинается сейчас.»*  
Готов вернуть систему к жизни? Хватай WinAntidote и раздави цифровой хаос! 🚀
